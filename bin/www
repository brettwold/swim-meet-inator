#!/usr/bin/env node

var models = require("../server/models");
var debug = require('debug')('SwimResultinator:server');
var http = require('http');
var ApiServer = require('../server');

var apiServer = new ApiServer();
apiServer.startServer();
